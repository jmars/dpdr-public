"""Experiment drivers (plan §7 Step C): exp1 failure thresholds, exp2 rescue
taxonomy, exp3 post-recovery characterization.  Run from the repo's `dpdr/`
directory (the parent of this package) as `python -m experiments.expN`."""
